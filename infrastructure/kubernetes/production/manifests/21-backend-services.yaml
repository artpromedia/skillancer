# =============================================================================
# Auth Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-svc
  namespace: skillancer
  labels:
    app: auth-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-svc
  template:
    metadata:
      labels:
        app: auth-svc
    spec:
      containers:
        - name: auth-svc
          image: skillancer/auth-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
          env:
            - name: PORT
              value: '3001'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: auth-svc
  ports:
    - port: 3001
      targetPort: 3001

---
# =============================================================================
# Market Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: market-svc
  namespace: skillancer
  labels:
    app: market-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: market-svc
  template:
    metadata:
      labels:
        app: market-svc
    spec:
      containers:
        - name: market-svc
          image: skillancer/market-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3002
          env:
            - name: PORT
              value: '3002'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 800m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: market-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: market-svc
  ports:
    - port: 3002
      targetPort: 3002

---
# =============================================================================
# SkillPod Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: skillpod-svc
  namespace: skillancer
  labels:
    app: skillpod-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: skillpod-svc
  template:
    metadata:
      labels:
        app: skillpod-svc
    spec:
      containers:
        - name: skillpod-svc
          image: skillancer/skillpod-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3003
          env:
            - name: PORT
              value: '3003'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 300m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1536Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: skillpod-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: skillpod-svc
  ports:
    - port: 3003
      targetPort: 3003

---
# =============================================================================
# Cockpit Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: cockpit-svc
  namespace: skillancer
  labels:
    app: cockpit-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cockpit-svc
  template:
    metadata:
      labels:
        app: cockpit-svc
    spec:
      containers:
        - name: cockpit-svc
          image: skillancer/cockpit-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3004
          env:
            - name: PORT
              value: '3004'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: cockpit-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: cockpit-svc
  ports:
    - port: 3004
      targetPort: 3004

---
# =============================================================================
# Billing Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-svc
  namespace: skillancer
  labels:
    app: billing-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billing-svc
  template:
    metadata:
      labels:
        app: billing-svc
    spec:
      containers:
        - name: billing-svc
          image: skillancer/billing-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3005
          env:
            - name: PORT
              value: '3005'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: billing-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: billing-svc
  ports:
    - port: 3005
      targetPort: 3005

---
# =============================================================================
# Notification Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-svc
  namespace: skillancer
  labels:
    app: notification-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-svc
  template:
    metadata:
      labels:
        app: notification-svc
    spec:
      containers:
        - name: notification-svc
          image: skillancer/notification-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4006
          env:
            - name: PORT
              value: '4006'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 400m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 4006
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4006
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: notification-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: notification-svc
  ports:
    - port: 4006
      targetPort: 4006

---
# =============================================================================
# Audit Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-svc
  namespace: skillancer
  labels:
    app: audit-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-svc
  template:
    metadata:
      labels:
        app: audit-svc
    spec:
      containers:
        - name: audit-svc
          image: skillancer/audit-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3012
          env:
            - name: PORT
              value: '3012'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3012
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3012
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: audit-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: audit-svc
  ports:
    - port: 3012
      targetPort: 3012

---
# =============================================================================
# Executive Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: executive-svc
  namespace: skillancer
  labels:
    app: executive-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: executive-svc
  template:
    metadata:
      labels:
        app: executive-svc
    spec:
      containers:
        - name: executive-svc
          image: skillancer/executive-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3007
          env:
            - name: PORT
              value: '3007'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: executive-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: executive-svc
  ports:
    - port: 3007
      targetPort: 3007

---
# =============================================================================
# Financial Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: financial-svc
  namespace: skillancer
  labels:
    app: financial-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: financial-svc
  template:
    metadata:
      labels:
        app: financial-svc
    spec:
      containers:
        - name: financial-svc
          image: skillancer/financial-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3008
          env:
            - name: PORT
              value: '3008'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: financial-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: financial-svc
  ports:
    - port: 3008
      targetPort: 3008

---
# =============================================================================
# Talent Graph Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: talent-graph-svc
  namespace: skillancer
  labels:
    app: talent-graph-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: talent-graph-svc
  template:
    metadata:
      labels:
        app: talent-graph-svc
    spec:
      containers:
        - name: talent-graph-svc
          image: skillancer/talent-graph-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3009
          env:
            - name: PORT
              value: '3009'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: talent-graph-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: talent-graph-svc
  ports:
    - port: 3009
      targetPort: 3009

---
# =============================================================================
# Intelligence Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: intelligence-svc
  namespace: skillancer
  labels:
    app: intelligence-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intelligence-svc
  template:
    metadata:
      labels:
        app: intelligence-svc
    spec:
      containers:
        - name: intelligence-svc
          image: skillancer/intelligence-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3010
          env:
            - name: PORT
              value: '3010'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 384Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3010
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3010
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: intelligence-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: intelligence-svc
  ports:
    - port: 3010
      targetPort: 3010

---
# =============================================================================
# Copilot Service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: copilot-svc
  namespace: skillancer
  labels:
    app: copilot-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: copilot-svc
  template:
    metadata:
      labels:
        app: copilot-svc
    spec:
      containers:
        - name: copilot-svc
          image: skillancer/copilot-svc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3011
          env:
            - name: PORT
              value: '3011'
            - name: HOST
              value: '0.0.0.0'
          envFrom:
            - configMapRef:
                name: skillancer-config
            - secretRef:
                name: skillancer-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3011
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3011
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: copilot-svc
  namespace: skillancer
spec:
  type: ClusterIP
  selector:
    app: copilot-svc
  ports:
    - port: 3011
      targetPort: 3011
