import { cn } from '../../lib/utils';

export interface {{name}}Props {
  /** Component children */
  children?: React.ReactNode;
  /** Additional CSS classes */
  className?: string;
  /** Optional variant */
  variant?: 'default' | 'primary' | 'secondary';
}

/**
 * {{name}} - A server component
 *
 * This component is rendered on the server and doesn't include any client-side
 * JavaScript. Add 'use client' directive if you need interactivity.
 */
export async function {{name}}({ children, className, variant = 'default' }: {{name}}Props) {
  // You can fetch data directly in server components
  // const data = await fetchSomeData();

  return (
    <div
      data-slot="{{kebabCase name}}"
      className={cn(
        // Base styles
        '{{kebabCase name}}',
        // Variant styles
        {
          'bg-background text-foreground': variant === 'default',
          'bg-primary text-primary-foreground': variant === 'primary',
          'bg-secondary text-secondary-foreground': variant === 'secondary',
        },
        className
      )}
    >
      {children}
    </div>
  );
}
