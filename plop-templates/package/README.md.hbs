# @skillancer/{{name}}

{{description}}

## Installation ```bash pnpm add @skillancer/{{name}}
``` Or in another workspace package: ```json { "dependencies": { "@skillancer/{{name}}":
"workspace:*" } } ``` ## Usage ```typescript import { example } from '@skillancer/{{name}}'; // Use
the package const result = example(); ``` ## API ### `example()` Returns a greeting string.
```typescript const greeting = example(); // => 'Hello from @skillancer/{{name}}' ``` ## Development
```bash # Build the package pnpm build # Build in watch mode pnpm dev # Run tests pnpm test # Type
check pnpm typecheck # Lint pnpm lint ``` ## License Private - Skillancer